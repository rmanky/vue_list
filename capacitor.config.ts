import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.rmanky.vuelist',
  appName: 'VueList',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
